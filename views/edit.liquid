{% layout "layout.liquid" %} {% block content %}
<div>
  <form action="/store/{{productL._id}}?_method=PUT" method="post">
    <fieldset>
      <legend>Edit {{productL.name}}</legend>
      <label>
        NAME:<input
          type="text"
          name="name"
          placeholder="enter fruit name"
          value="{{productL.name}}"
        />
      </label>
      <label>
        COLOR:<input
          type="text"
          name="color"
          placeholder="enter fruit name"
          value="{{productL.color}}"
        />
      </label>
    </fieldset>
    <input type="submit" value="Edit {{productL.name}}" />
  </form>
</div>
{% endblock %}