{% layout "layout.liquid" %} {% block content %}
<div>
  {% for item in product %}
  <article>
    <a href="/store/{{ item._id }}">
      <h2>
        {{item.name}} 
      </h2>
    </a>
  </article>
  {% endfor %}
</div>
{% endblock %}
