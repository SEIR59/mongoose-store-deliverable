{% layout "layout.liquid" %} {% block content %}
<div>
  <a href="/store/new"><button> Add another product</button></a>
  {% for productL in products %}
  <article>
    <a href="/store/{{ productL._id }}">
      <h2>
        {{productL.name}}
      </h2>
    </a>
  </article>
  {% endfor %}
</div>
{% endblock %}
